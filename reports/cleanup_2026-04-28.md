# Sub3D — Audit d'assainissement

**Date**: 2026-04-28
**Auteur**: Claude (audit read-only, aucune modification)
**Branche**: scripting
**HEAD**: 17a3f7b Revert "fix(net): authoritative pose source for rebase + SimProxy GridSpace smoothing"
**Référence autorité-max**: `reports/plans/2026-04-10_first_playable_strategic_analysis.md`

---

## 0. TL;DR

L'arbre de travail est **plus sain que la mémoire ne le suggère**. 47 fichiers modifiés (+1520 / -633), une dizaine d'untracked. **Le build compile cleanly (exit 0)**. Le diff massif n'est PAS du chaos post-revert : c'est du travail cohérent avec les plans documentés.

Trois constats clés :

1. **Phase A + Phase 1 playback + Phase C du master refactor sont déjà appliqués** dans le working tree, conformes aux plans.
2. **Système Ladder + procedural anim + Sub3DDebugPanel + instrumentation motion-chain** sont écrits, compilent, attendent juste validation in-game.
3. **3 items critiques** demandent ta décision (§3) avant que je propose des commits.

Les "petits bugs" évoqués sont probablement des bugs de validation (asset wiring, comportement runtime), PAS des incohérences de code. La phase 2 (correction) ne devrait pas être un nettoyage de code mais un **plan d'attaque ciblé sur les bugs runtime que tu listeras**.

---

## 1. Inventaire git

### 1.1 Commits récents (du plus récent au plus ancien)
- `17a3f7b` Revert "fix(net): authoritative pose source for rebase + SimProxy GridSpace smoothing"
- `f568439` fix(net): authoritative pose source for rebase + SimProxy GridSpace smoothing ← **annulé par le revert**
- `b531e24` fix(net): SimProxy crew smoothing + sub snapshot interpolation + Gameplay Debugger
- `b866020` fix(net+bootstrap): jitter resolution + 3-layer smoothing pipeline
- `716d1a2` fix(crew): make LastSubWorldTransform public for init paths

Le revert (17a3f7b → f568439) annule UNE tentative de fix. La mémoire `project_motion_chain_jitter_root_cause_2026_04_27.md` indique que la vraie cause était côté Project Settings, pas côté code → le revert est cohérent.

### 1.2 Diff stat global
- 47 fichiers modifiés, +1520 / -633 lignes.
- 2 fichiers supprimés : `SubInteriorFrameComponent.{cpp,h}` (Phase C, attendu).
- ~14 untracked notables (Ladder system, plugin Sub3DDebugPanel, water material, voxel pack, 5 plan docs).

---

## 2. Cartographie du diff (par intent)

| Lot | Fichiers | État | Plan associé |
|---|---|---|---|
| **A** Phase A — Replication contract | `SubmarineRuntimeTypes.h`, `SubMovementComponent.{h,cpp}`, `SubmarineBase.cpp` | **Appliqué + compile** | `2026-04-27_unified_motion_chain_master_refactor.md` §6 Phase A |
| **B** Phase 1 — Buffered playback non-auth | `SubMovementComponent.{h,cpp}` | **Appliqué + compile** | `2026-04-27_submovement_non_authority_playback_refactor_and_audit.md` |
| **C** Phase C — Kill InteriorFrame | `SubInteriorFrameComponent.{h,cpp}` (supprimés), `SubmarineBase.{h,cpp}`, `SubCrewMovementComponent.{h,cpp}`, `SubCrewCharacter.cpp`, `Diagnostics/SubRelativeFrameExpectedTransformProvider.{h,cpp}`, `Sub3DDebugSettings.h`, `BP_Submarine_Craniata.uasset` | **Appliqué + compile, aucun orphan** | `2026-04-27_unified_motion_chain_master_refactor.md` §5.3, §6 Phase C |
| **D** Système Ladder | NEW: `Ladder.{h,cpp}`, `LadderClimbComponent.{h,cpp}`. MOD: `SubCrewMovementComponent.{h,cpp}` (+ replication LadderClimbProgress01, BeginLadderClimb/EndLadderClimb/TickLadderClimb), `BP_Ladder.uasset` | **Appliqué + compile, non validé in-game** | `2026-04-27_ladder_climb_system.md` |
| **E** Procedural anim | `AnimNode_CrewProcedural.{h,cpp}` (+91 lignes), `SubCrewAnimInstance.{h,cpp}` | **Compile, non validé** | Roadmap post-helm #4 |
| **F** Plugin Sub3DDebugPanel | NEW: `Plugins/Sub3DDebugPanel/**` (Slate dockable Editor module) | **Compile (binaires présents)** | (pas de plan, ajout outillage) |
| **G** Motion chain instrumentation | `Sub3DDebugSettings.h` (+53 lignes), `SubCrewMovementComponent.{h,cpp}`, `SubMovementComponent.{h,cpp}` (toggles + log helpers) | **Appliqué + compile** | Audit `2026-04-27_motion_conditional_jitter_root_cause_audit.md` §13 |
| **H** Compartment probe channel | `Config/DefaultEngine.ini` (CompartmentProbe + profiles) | **Persisté** | `2026-04-22_crew_environment_axis.md` |
| **I** Stair → Ramp closure | `CLAUDE.md` (env debt removed), `SM_Stair_*.uasset` (collision rework) | **Persisté** | (closure de la dette listée dans CLAUDE.md) |
| **J** Defaults debug Game.ini | `Config/DefaultGame.ini` (`bDrawCompartmentVolumes=True`, etc.) | **Persisté** | (toggles dev) |
| **K** Water material | NEW: `M_CompartmentWater.uasset`, `MI_CompartmentWater.uasset`, `Material/Functions/MF_CompartmentWater_*.uasset` (3 functions), 6 scripts UnrealClaude d'inspection | **Untracked, scope incertain** | Roadmap post-helm #1 |
| **L** Voxel abyss pack | NEW: `Scripts/Blender/voxel_abyss_pack/` (15+ scripts: outposts, mobs, flora, vent_fields, stalactites…), `run_voxel_abyss_pack.py` | **Untracked, parallèle au gameplay** | (pas de plan dans `reports/`) |
| **M** Plans & assets divers | 5 plan docs `2026-04-27_*.md`, `BP_DEbugPassenger.uasset`, `BP_Light.uasset`, `.tmp_frames/` (instrumentation pacing 26 avril) | **Untracked** | — |

**Lecture** : les lots A → I sont du travail fini, cohérent et compilable. Les lots K → M sont du WIP ou de l'outillage que tu as accumulé ailleurs.

---

## 3. Items critiques (action requise)

### 3.1 🔴 `bUseFixedFrameRate=False` — fix documenté NON ACTIF

**Constat**:
- Mémoire `project_motion_chain_jitter_root_cause_2026_04_27.md` : « 2-weeks blocked sub jitter at trigger events fixed by Project Settings → Use Fixed Frame Rate 60 ».
- Diff `Config/DefaultEngine.ini` ajoute `bUseFixedFrameRate=False` + `FixedFrameRate=60.000000`.
- Code [SubMovementComponent.cpp:118-122](Source/Sub3D/Submarine/SubMovementComponent.cpp:118) émet un Warning au BeginPlay si `GEngine->bUseFixedFrameRate` est false. Ce warning va donc tirer à chaque PIE.

**Possibilités** :
- Tu as testé le fix puis désactivé pour vérifier qu'il était bien le coupable (saine science) → re-cocher la case.
- Le toggle a sauté pendant un cycle d'éditeur → re-cocher.
- Tu juges que le fix ne tenait pas → la mémoire est obsolète, à mettre à jour.

**Action proposée** : Project Settings → Engine → General Settings → Framerate → cocher "Use Fixed Frame Rate" → vérifier que `DefaultEngine.ini` passe à `bUseFixedFrameRate=True`. Sinon, dis-moi quoi mettre à jour dans la mémoire.

### 3.2 🟡 Fragmentation des plans 2026-04-27 — 5 docs, 1 seul reste utile

**Constat** : 5 docs `2026-04-27_*.md` non-trackés, dont 4 sont devenus stales après la découverte de la cause framerate :

| Plan | Statut effectif | Recommandation |
|---|---|---|
| `motion_conditional_jitter_root_cause_audit.md` | **Résolu** (§13 ajouté en fin avec la résolution) | Garder comme cas d'étude — référence pour `feedback_legacy_after_refactor` |
| `submovement_non_authority_playback_refactor_and_audit.md` | Phase 1 appliquée et utile | Garder comme historique |
| `unified_client_presentation_chain_refactor.md` | Path 2 draft, **superseded** par master refactor | Supprimer ou marquer SUPERSEDED |
| `unified_motion_chain_master_refactor.md` | Phase A appliquée, Phase C appliquée, Phase B/D **deferred** post-fix root cause | Garder — Phase B/D restent valides comme amélioration qualité |
| `ladder_climb_system.md` | Code écrit, validation in-game pending | Garder (référence active) |

**Action proposée** : commiter les 5 plans tels quels pour traçabilité, ajouter un en-tête `## Status: SUPERSEDED` au `unified_client_presentation_chain_refactor.md`. Pas de suppression.

### 3.3 🟡 `.tmp_frames/` non gitignoré (~3 MB de captures + scripts d'analyse)

**Constat** : 28 .jpg + 3 .py (analyze_pacing.py, etc.). Outillage de l'investigation framerate. Date 2026-04-26 = avant la résolution.

**Action proposée** :
- Soit ajouter `.tmp_frames/` au `.gitignore` (garder local pour réf future).
- Soit supprimer (le .py d'analyse pourrait re-servir → préfère gitignore).

---

## 4. Travail cohérent prêt à committer (ordre proposé)

Chaque commit est atomique et revertable. Tu peux dire "go" sur l'ordre, l'ajuster, ou en exclure.

### Commit 1 — `chore(config): persist project debug defaults + CompartmentProbe channel`
- `Config/DefaultEngine.ini` (CompartmentProbe + profiles) — lot **H**
- `Config/DefaultGame.ini` (debug toggles persistés) — lot **J**
- ⚠️ **Décision préalable §3.1** : avec ou sans `bUseFixedFrameRate=True`.

### Commit 2 — `feat(net): replication contract Phase A + buffered playback Phase 1`
- Lot **A** + **B** (replication + buffered playback)
- Fichiers : `SubmarineRuntimeTypes.h`, `SubMovementComponent.{h,cpp}`, `SubmarineBase.{h,cpp}`
- Pas de changement BP requis.

### Commit 3 — `refactor(crew): kill USubInteriorFrameComponent (Phase C)`
- Lot **C** entier
- Suppression `SubInteriorFrameComponent.{h,cpp}`
- Migration `SubCrewCharacter.cpp`, `SubCrewMovementComponent.{h,cpp}`, `SubmarineBase.{h,cpp}`, `Diagnostics/SubRelativeFrameExpectedTransformProvider.{h,cpp}`, `Sub3DDebugSettings.h`
- Asset re-saved : `BP_Submarine_Craniata.uasset` (slot orphan effacé)
- Mettre à jour `CLAUDE.md` § Crew embarked movement pour retirer la mention InteriorFrame du tick prereq.

### Commit 4 — `feat(crew): ladder climb system (post-FP roadmap #3)`
- Lot **D**
- Nouveau : `Ladder.{h,cpp}`, `LadderClimbComponent.{h,cpp}`, `BP_Ladder.uasset`
- Modif : `SubCrewMovementComponent.{h,cpp}` (BeginLadder/EndLadder/Tick + replication)
- Inclure le plan `2026-04-27_ladder_climb_system.md`
- ⚠️ Validation in-game pending — commit possible mais marquer "untested" dans le message.

### Commit 5 — `feat(anim): procedural crew anim node (single node, 18 bones)`
- Lot **E**
- `AnimNode_CrewProcedural.{h,cpp}`, `SubCrewAnimInstance.{h,cpp}`
- ⚠️ Validation in-game pending.

### Commit 6 — `feat(editor): Sub3DDebugPanel plugin (dockable Slate panel)`
- Lot **F**
- `Plugins/Sub3DDebugPanel/**` (sources uniquement, pas Binaries/Intermediate qui sont gitignored par règle générale).
- Module Editor-only Win64.

### Commit 7 — `feat(debug): unified motion-chain instrumentation`
- Lot **G**
- `Sub3DDebugSettings.h` (toggles bLogPresentationChain, bLogMotionChainTrace, kill-switches)
- Helpers de log dans SubMov / SubCrewMov
- Mémoire `project_motion_chain_jitter_root_cause_2026_04_27.md` recommande de garder l'instrumentation pour le prochain bug réplication.

### Commit 8 — `chore(repo): close stair env debt + ignore .tmp_frames/`
- Lot **I** (CLAUDE.md mise à jour, `SM_Stair_*.uasset` collision rework déjà persistées)
- Ajouter `.tmp_frames/` au `.gitignore` (§3.3)

### Commit 9 — `docs: cleanup audit + 5 plans 2026-04-27`
- 5 plan docs (avec en-tête SUPERSEDED sur le `unified_client_presentation_chain_refactor.md`)
- Ce rapport `cleanup_2026-04-28.md`

---

## 5. Travail spéculatif/incomplet (à clarifier avant de committer)

### 5.1 Lot K — Water material (3 MF + M + MI + 6 scripts UnrealClaude)
- Cohérent avec roadmap post-helm #1 (water mat HLSL + clipping coque).
- 6 scripts d'inspection UnrealClaude dans `Content/UnrealClaude/Scripts/` — outillage exploratoire, à clean une fois l'authoring fini.
- Assets `f124d7a1-…_normal.uasset`, `water_0397cbormal.uasset`, `watergemininormal2_specular__1_.uasset` — 3 normales avec noms générés/copiés, à renommer proprement avant commit.

**Question** : tu veux quoi avec ces assets ? Commit en l'état, attendre que l'authoring water mat soit fini, ou supprimer les normales générées pour repartir propre ?

### 5.2 Lot L — Voxel abyss pack
- Package Blender complet (outposts, mobs, flora, vent_fields, stalactites, voxel_craniata, submarine_modular).
- Pas de plan dans `reports/` qui le référence — pas dans la roadmap post-helm.
- Outillage de génération de contenu, séparé du gameplay.

**Question** : ça vit où ? `Scripts/Blender/voxel_abyss_pack/` est OK pour committer, ou c'est du WIP perso à garder local ?

### 5.3 Untracked BP `BP_DEbugPassenger.uasset` + `BP_Light.uasset`
- Nom typoé (`DEbugPassenger`).
- Aucune réf depuis le code (grep clean).

**Question** : encore utile ou supprimable ?

---

## 6. Build & validation status

| Check | Statut |
|---|---|
| `Build.bat Sub3DEditor Win64 Development` | ✅ Exit 0 |
| Orphan refs à `USubInteriorFrameComponent` en code | ✅ Aucune (1 mention dans un commentaire de doc) |
| Orphan refs aux input replications retirées (`UPROPERTY(Replicated)` sur RudderInput etc.) | ✅ Champs réécrits depuis NetState dans HandleReplicatedNetState |
| Ladder ↔ CrewMovement wiring | ✅ `BeginLadderClimb` / `EndLadderClimb` / `TickLadderClimb` implémentés, replication COND_SkipOwner OK |
| `bUseFixedFrameRate` runtime guardrail | ✅ Warning émis BeginPlay si False (SubMovementComponent.cpp:118) |
| Validation in-game (PIE) | ❓ Hors scope phase 1 — à faire entre Commit 3 et Commit 4 |

---

## 7. Ce qui n'a PAS été détecté comme bug

L'audit n'a trouvé aucune incohérence de code. Donc les "petits bugs" que tu mentionnes ne sont probablement pas dans ce que je peux voir en read-only. Candidats à reproduire en PIE :

1. **Régression jitter** si `bUseFixedFrameRate=False` est l'état actuel (§3.1).
2. **Ladder system** non validé in-game — placement BP_Ladder dans Craniata, test de la traversée verticale, replication peer.
3. **Procedural anim** non validé — comportement des 18 bones via le nouveau nœud unique.
4. **Phase B/D du master refactor non appliquées** — selon ton ressenti après cleanup, certains résiduels de jitter (rudder visual, peer crew) pourraient encore exister. Plan disponible si besoin.

Quand tu seras en PIE pour valider, fais une liste concrète des symptômes (un par ligne, avec sévérité bloquant/gênant/cosmétique) et on attaque. Le code est sain ; les bugs sont en aval.

---

## 8. Décisions demandées

Pour passer en phase 2 (cleanup réel), j'ai besoin de tes réponses sur :

1. **§3.1 `bUseFixedFrameRate`** — toggle ON ou mémoire à mettre à jour ?
2. **§3.2 Plans 2026-04-27** — OK pour commit + en-tête SUPERSEDED sur le unified_client_presentation_chain ?
3. **§3.3 `.tmp_frames/`** — gitignore ou supprimer ?
4. **§5.1 Water material** — committer en l'état, attendre, ou clean d'abord ?
5. **§5.2 Voxel abyss pack** — local ou commit ?
6. **§5.3 BPs orphelins (DEbugPassenger, Light)** — utiles ou à supprimer ?
7. **Ordre des commits §4** — go par défaut, ou tu veux changer l'ordre / fusionner / split ?

Une fois ces 7 décisions prises, je peux exécuter la phase 2 (cleanup par commits ciblés) en autonomie. Si tu veux tester chaque commit en PIE entre temps, on découpe par paquets et tu valides au fur et à mesure.
